// Popup entry point
import { EXTENSION_CONFIG } from '../constants';

console.log(`${EXTENSION_CONFIG.NAME} v${EXTENSION_CONFIG.VERSION} popup loaded`);

// Initialize popup
document.addEventListener('DOMContentLoaded', () => {
  console.log('Popup initialized');
  // TODO: Implement popup UI
});
